version: '3'

services:
  portainer:
    image: portainer/portainer-ce
    container_name: portainer
    security_opt:
      - no-new-privileges:true
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./portainer-data:/data
    ports:
      - 9000:9000
    restart: unless-stopped
    networks:
      - proxy

  cloudflare-ddns:
    image: oznu/cloudflare-ddns:latest
    container_name: ddns
    env_file: .env
    restart: unless-stopped
    environment:
      - API_KEY=${DNSAPI}
      - ZONE=${SITE}
      - PROXIED=true
      - PUID=1001
      - PGID=1001
    networks:
      - proxy

  nginx:
    image: 'jc21/nginx-proxy-manager:latest'
    ports:
      - '80:80'
      - '81:81'
      - '443:443'
    environment:
      DB_MYSQL_HOST: "db"
      DB_MYSQL_PORT: 3306
      DB_MYSQL_USER: "npmchangeme"
      DB_MYSQL_PASSWORD: "npmchangeme"
      DB_MYSQL_NAME: "npmchangeme"
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt
  db:
    image: 'jc21/mariadb-aria:latest'
    environment:
      MYSQL_ROOT_PASSWORD: 'npmchangeme'
      MYSQL_DATABASE: 'npmchangeme'
      MYSQL_USER: 'npmchangeme'
      MYSQL_PASSWORD: 'npmchangeme'
    volumes:
      - ./data/mysql:/var/lib/mysql

  speedtest:
    image: openspeedtest/latest:latest
    container_name: openspeedtest
    ports:
      - 3005:3000
      - 3006:3001
    restart: unless-stopped
    networks:
      - proxy
      
  dashboard:
    image: b4bz/homer
    container_name: homer-dashboard
    env_file: .env
    ports:
      - 9055:8080
    restart: unless-stopped
    environment:
      - USER=${USER}
      - GROUP=${USER}
      - UID=1001
      - GID=1001
    volumes:
      - ./homer:/www/assets:ro
    networks:
      - proxy

  grafana:
    image: grafana/grafana-oss:latest
    container_name: grafana
    ports:
      - 4020:3000
    volumes:
      - grafana-data:/var/lib/grafana
    restart: unless-stopped
    networks:
      - proxy
 
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - 9010:9090
    volumes:
      - ./prometheus:/etc/prometheus
      - prometheus-data:/prometheus
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
    networks:
      - proxy
    
  node_exporter:
    image: quay.io/prometheus/node-exporter:latest
    container_name: node_exporter
    command:
      - '--path.rootfs=/host'
    pid: host
    restart: unless-stopped
    volumes:
      - '/:/host:ro,rslave'
    networks:
      - proxy

  cadvisor:
    image: google/cadvisor:latest
    container_name: cadvisor
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    devices:
      - /dev/kmsg
    restart: unless-stopped
    networks:
      - proxy

networks:
  proxy:
    external: true

volumes:
  prometheus-data:
    driver: local
  grafana-data:
    driver: local